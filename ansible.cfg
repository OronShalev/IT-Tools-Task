[defaults]
roles_path       = $HOME/IT-Tools-Task/roles
inventory        = $HOME/IT-Tools-Task/inventory
forks            = 10
remote_user      = automation
private_key_file = $HOME/.ssh/id_rsa

[privilege_escalation]
become = false